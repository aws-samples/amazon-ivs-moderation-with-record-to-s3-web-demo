import { styled, Typography } from "@material-ui/core"


const TitleWithIcon = styled(Typography)({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  gap: '2rem',
})

export {
  TitleWithIcon
}